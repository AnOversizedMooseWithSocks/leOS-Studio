three.js goes here.

On first run `python app.py` downloads three.js r128 into this folder and the app is fully offline
from then on. If this machine has no internet, download three.min.js (r128) on another machine and
save it here as three.min.js -- the app loads this copy before trying any CDN.
